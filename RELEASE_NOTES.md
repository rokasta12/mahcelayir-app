## v0.1.1

- Auto-updater: app checks for updates on launch and can install them from the Settings page
- Error boundary protects projects if a component crashes
- PDF generation skips unreadable images instead of failing the whole catalog
- Versioned data migrations keep your projects safe across updates
- PDF viewer re-fits correctly when the panel resizes
- Removing an artwork now asks for confirmation
- Many small polish fixes
